pipeline:
  build:
    image: busybox
    commands:
      - echo hi
  docker:
    debug: true
    repo: vespakoen/drone-docker-compose
    tag: development
    storage_driver: vfs
    insecure: true
    environment:
      PLUGIN_DNS: "127.0.0.11"
      PLUGIN_BIP: "true"
