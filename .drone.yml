pipeline:
  build:
    image: busybox
    commands:
      - echo hi
  docker:
    repo: vespakoen/drone-docker-compose
    tag: development
    # insecure: true
    environment:
      - DOCKER_LAUNCH_DEBUG="true"
